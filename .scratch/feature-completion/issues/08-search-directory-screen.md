# 08 — Search & Directory Navigation Tab (`SearchScreen`)

**What to build:**
A dedicated **Search & Directory** screen accessible via the 4-destination bottom `NavigationBar`. Presents a text search bar and filter chips (Title, Tag, List, Future/Background routines like *Descaling Coffee Machine*) to browse all routines before they become due.

**Blocked by:** 01 — Task Creation Bottom Sheet Modal & Persistence

**Status:** closed

- [x] Add `Search` tab to 4-item bottom `NavigationBar` (`Mission Control ⚡`, `Search 🔍`, `Challenges 🏆`, `Stats 📊`).
- [x] Build `SearchScreen`Compose view with text search field and filter chips.
- [x] Render all active, reusable, and future background routines matching search query.
