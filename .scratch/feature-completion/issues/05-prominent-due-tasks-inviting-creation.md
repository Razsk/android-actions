# 05 — Prominent Due Objectives HUD Card & Inviting Task Creation

**What to build:**
1. A prominent **DUE OBJECTIVES PROTOCOLS** HUD card on Mission Control rendered directly below the header, displaying open due routines and tasks with quick completion checkboxes and postponement buttons.
2. An inviting **Solid Action Blue (+ CREATE TASK)** header button and an **Action Blue Floating Action Button (FAB)** anchored at bottom-right.
3. Inline **+ NEW TAG** and **+ NEW LIST** creation chip buttons inside `TaskCreationBottomSheet` to define custom tags and lists on the fly.
4. A **REUSABLE TASK** Material 3 `Switch` toggle inside `TaskCreationBottomSheet` setting `TaskEntity.isReusable`.

**Blocked by:** 01 — Task Creation Bottom Sheet Modal & Persistence

**Status:** closed

- [x] Prominent 'DUE OBJECTIVES PROTOCOLS' HUD card rendered below header on Mission Control.
- [x] Solid Action Blue header button and Floating Action Button (FAB) launch task creation modal.
- [x] Inline '+ NEW TAG' and '+ NEW LIST' chip buttons with text inputs inside creation modal.
- [x] Material 3 Switch toggle for 'REUSABLE TASK' persisting `isReusable` on `TaskEntity`.
